#include<stdio.h>

int main()
{
    int n,w,h,x,y;
    scanf("%d",&n);
    scanf("%d",&w);
    scanf("%d",&h);

    for (x=0; x<=h,x++;)
    {
        for(y=0; y<=w,y++;)
        {
            printf("%d",n);
        }

    }
    return 0;
}
